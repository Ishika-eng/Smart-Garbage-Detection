# Database Configuration
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=garbage_detection_db

# ML Model Path
MODEL_PATH=path/to/best.pt

# Upload Directory (optional, defaults to backend/uploads)
# UPLOAD_DIR=backend/uploads
